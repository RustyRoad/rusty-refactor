# Rusty Refactor Windows Audio

This UI-side companion keeps default-microphone capture on the Windows client
when the main Rusty Refactor extension runs in a Remote-SSH workspace.

The companion exposes internal VS Code commands to the workspace extension.
It does not use a terminal, PowerShell, or a network listener.
